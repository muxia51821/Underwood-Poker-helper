# 版本更新说明

## V7.7

- **V7.7.2** — Quiz 独立可用 + 文档更新
    - **Quiz 独立初始化**：`renderDiscover()` 中将 Quiz init 提前到发现结果渲染之前，无手牌导入时 Quiz 也能正常使用
    - **"针对训练"→"Quiz"**：卡片标题和按钮文字统一改为 Quiz
    - **文档更新**：CLAUDE.md（版本/基线/依赖/Memory）、ARCHITECTURE.md（模块/DAG/Schema/导航/数据流）、新增项目根目录 README.md

- **V7.7.1** — SBvsBB 真实数据替换 + High Card 过滤器恢复
    - **SBvsBB 数据替换**：从用户提供的 GTO Solver 输出（MD 表格）重新生成 `SBvsBB_SRP_flop.js`（183 boards），旧数据为 BTNvsBB 副本现已分化（如 TsTdTc：SB bet33=80.76% vs BTN bet33=91.55%）
    - **High Card 过滤**：GTO 速查表恢复高牌下拉（A/K/Q/J/T-High + Low），`srpData.js` 新增 `_getHighCard()` 从 boardCode 提取最高 rank
    - **三过滤器组合**：场景 × 高牌 × 牌面类型，可任意组合过滤

- **V7.7.0** — GTO 数据整合 + 策略速查表重构
    - **单一数据源**：`srpData.js` 从 818 行硬编码重写为 35 行自动生成脚本，直接从 `gtoRaw/` 两个场景文件衍生 367 条数据（-783 行代码）
    - **速查表改版**：标题从"SB v BB SRP Flop Strategy"→"GTO 翻牌频率速查"，英文 4 过滤器替换为场景选择 + 中文 9 类牌面类型过滤
    - **表格列重构**：Flop（card-badge）| 类型 | 主流行动（简写+频率%+微型柱状条）| 次选行动
    - **行动简写**：BB=大注75%、BM=中注50%、BS=小注33%、X=过牌；三色判定（绿≥35% / 黄 13-35% / 红<13%）与 Quiz 一致
    - **构建产物**：340kB→330kB（-10kB），净减 ~780 行代码

## V7.6

- **V7.6.5** — 四项小修复
    - **Hand 编辑场景下拉**：新增 `#handPreflopScenario` 选择器（11 种 6-max 对抗），editHandReview 回填值、saveHandReview 写入
    - **Discover 扫描锁修复**：`scan()` 加 `try-finally` 确保 `_scanning` 异常时仍释放
    - **图表指纹跳过重绘**：`renderCharts()` 新增手牌数+最后 ID 指纹，数据不变则跳过 Canvas 重绘
    - **卡牌花色亮底统一**：`.card-badge.black` 和 `.card-badge.red` 背景统一为 `#e8e8e8`

- **V7.6.4** — Quiz 出题策略升级：掌握降权 + 轮转均匀分布
    - **掌握追踪**：新增 `pa_quiz_mastery` 存储，每题独立追踪连续答对次数，连续 2 次 correct → `mastered: true`
    - **轮转均匀分布**：按 9 类牌面分组，每次选最久未被抽到的 category；类内 80% 从未掌握中抽、20% 抽查已掌握
    - **错题优先**：有错题时从错题池按轮转逻辑出题
    - **全部掌-握自动重置**：某 category 下所有题都掌握后清除该 scenario mastery 标记
    - `next()` 拆分为 5 个函数：`next` + `_pickFromErrors` + `_pickFromPool` + `_pickByRotation` + `_buildQuestion`

- **V7.6.3** — Quiz 判分逻辑双阈值升级
    - **三区判定**：≥35% 正确（绿）、13-35% 可接受（黄）、<13% 错误（红）
    - **多正确选项提示**：所有 ≥35% 的动作自动列出（如"主流包括 过牌(61.5%)、大注(41.5%)"）
    - **错题集行为**：correct→移除最早一条、acceptable→不增不减、wrong→新增记录
    - `answer()` 返回值新增 `result`/`message`/`color` 字段，UI 反馈三色渲染

- **V7.6.2** — Quiz 错题集
    - **错题记录**：答错自动存入 `pa_quiz_errors`（boardCode + GTO 频率 + 用户答案 + 时间戳）
    - **错题列表**：按牌面类型分组，组内时间倒序；每条显示牌面 badge、你的答案 vs GTO 推荐、相对时间
    - **重新作答**：原地展开答题区，`next()` 新增 `targetBoardCode` 参数支持精确出题
    - **删除策略**：答对仅删最早一条（连续答对多次才能彻底清空）；支持手动单条删除 + 一键清空
    - **Discover 联动**：每条错题显示该牌面类型+场景的实战手牌数和平均盈亏
    - **交互**：移动端左滑删除（touchstart/touchend 检测）+ PC 端悬停显示 ×

- **V7.6.1** — Discover 热力图
    - **双视图切换**：盈亏 BB/手（绿→白→红）+ CBet 偏离%（蓝→白→红）
    - **Canvas 热力图**：横轴 9 类牌面 × 纵轴位置对抗，每格显示数值 + 颜色深浅
    - **Hover tooltip**：白框高亮 + 显示手数/盈亏/CBet/GTO 参考
    - **GTO 数据兼容**：仅 BTNvsBB/SBvsBB 有 GTO 参考时显示 CBet 偏离，其余灰显 N/A
    - **数据层**：`Discover.getHeatmapData()` 返回完整 category×scenario 网格

- **V7.6.0** — Monte Carlo 资金波动模拟
    - **模拟引擎**：`_runMonteCarlo()` 从真实盈亏分布采样 10000 轮，JS 纯实现
    - **Canvas 直方图**：30 bin，绿柱(盈利区)/红柱(亏损区)/虚线零线
    - **百分位输出**：P5/P25/P50/P75/P95 + 盈利概率一句话总结
    - **手数切换**：500/1000/5000/10000，点击重跑模拟
    - **边界处理**：<30 手显示"数据不足"

## V7.5

- **V7.5.1** — 位置对抗速查 + 字体优化 + 翻前弃牌过滤 + 卡牌花色 + /simplify
    - **位置对抗速查**：Hand 编辑区行动线速查下方新增折叠卡片，10 种 6-max 对抗 GTO 策略要点，`preflopScenario` 自动匹配
    - **字体优化**：`.session-table` 0.7→0.82em、`.card-badge` 0.7→0.85em、padding 加宽
    - **翻前弃牌过滤**：Discover 三层过滤（boardCategory + actionLineOTF + Hero preflop fold 检测），排除翻前弃牌手牌污染
    - **卡牌花色**：♠♣ 亮底黑字 `#d4d4d4/#1a1a1a`、♥♦ 亮底红字 `#f0e0e0/#cc2233`，全局统一
    - **/simplify**：`_formatBoard`→`Utils.renderCardBadges()` 去重；删 `patched` 死变量
    - **场景感知**："针对训练"按钮传 `scenario|category`，Quiz 同时预设场景选择器（BTNvsBB/SBvsBB）和牌面阶段

## V7.4

- **V7.4.9** — Discover 等分双列 + 场景感知自我矛盾 + 牌面分类补全
    - **等分双列**：Discover 和 Quiz 改用 `.sub-panel` 原生 `grid: 1fr 1fr` 布局；Quiz 包裹 `.card` 外壳视觉统一
    - **场景感知**：自我矛盾检测从"vs 全局平均"改为"vs 同场景平均"
    - **牌面补全**：`Utils.classifyBoard()` 新增 `made_straight`（天顺面）和 `trips_board`（三条面）检测；quizTrainer/discover 同步补标签
- **V7.4.8** — Discover 数据层打通：GTO 基准接入 + Quiz 进度回传
    - **Type 3 激活**：discover.js import `detectExtremes` + `getGTOReference`；GTO 极端偏离检测从死代码变为实际调用
    - **GTO 参考**：自我矛盾发现追加 GTO CBet 参考值（如 `GTO 参考 CBet 55%`）
    - **Quiz 回传**：发现列表根据 Quiz 训练准确率标记 🔴 弱项（<50%）或 ✅ 已掌握（>80%）
- **V7.4.7** — 代码去重 + Quiz 迁移 Discover + 稳定性修复
    - **函数去重**：`classifyBoard()` + `extractActionLine()` 统一收口到 `utils.js`；删除 quizTrainer/store/discover 3 处本地副本（-260 行）；ggParser 委托 Utils；删死代码 `renderFreqExplorer`（-115 行）
    - **Quiz 迁移**：GTO Quiz 从 Odds 面板移到 Discover 面板右侧（≥960px 双列 3:2）；`_bindQuizUI` 移入 review.js；Quiz init 从 odds.js 解耦
    - **Canvas 零宽**：`_renderCumulativeProfitChart` W<10px 时跳过渲染
    - **putAll**：收集单条写入失败 + 始终校验写入数量（移除 DEV 门控）
    - **Discover**：`_scanning` 防重入锁 + 缓存 + 监听器泄漏修复（container/Quiz bind 加一次守卫）
    - **ggParser**：`_posMap` 恢复（sed 误删）+ `_detectScenario` 修复（sed 污染）
    - **CLAUDE.md**：补编码规则 4 条（self/this/callback/init）+ 验证流程（Live Server/Console 零报错）+ 文件结构 + UI 导航更新
- **V7.4.6** — Discover 自动模式发现引擎
    - **新子 Tab**：Review → Discover，替代旧 Frequency Explorer 手动筛选器
    - **discover.js**：`_scanPatterns()` 三种自动发现 — 盈亏异常（优先级最高）、自我矛盾（与全局频率偏差 > 20%）、偏离 GTO
    - **发现列表**：每条显示类型/标题/手数/频率对比/盈亏 + [查看手牌] 按钮
    - **手牌跳转**：点击手牌行 → 切 Hand Tab → 定位编辑
    - **空状态**：少于 50 手显示提示
- **V7.4.5** — 行动线提取 + Freq Explorer 手牌表格 + 点击跳转
    - **行动线提取**：`ggParser._extractActionLine()` 从 desc 提取 OTF/OTT/OTR 行动序列短码（B60-C / X-X 等），新手牌自动填充
    - **手牌表格**：展开手牌列表改为标准表格（Time / Type / Session / Hand / Profit / Edit），含 Session 名 + Hero 底牌花色徽章
    - **事件委托**：`#freqDetail` click 委托处理行点击 + ✎ 按钮
    - **点击跳转**：点击手牌行/✎ → 切 Hand Tab → 翻页定位 → `editHandReview`
    - **行动线提取**：`ggParser._extractActionLine()` 从 desc 提取 OTF/OTT/OTR 行动序列短码（B60-C / X-X 等），新手牌自动填充
    - **手牌表格**：展开手牌列表改为标准表格（Time / Type / Session / Hand / Profit / Edit），含 Session 名 + Hero 底牌花色徽章
    - **事件委托**：`#freqDetail` click 委托处理行点击 + ✎ 按钮
    - **点击跳转**：点击手牌行/✎ → 切 Hand Tab → 翻页定位 → `editHandReview`
- **V7.4.4** — Bug 三联修复：Session 级联删除 + 4MB 误报 + Freq Explorer 手牌展开
    - **A 级联删除**：删除 Session 时同步删除关联手牌（marked 精选手牌保留）
    - **B 存储配额**：`checkStorageQuota` 改用 `navigator.storage.estimate()` 检测 IndexedDB 配额，消除 4MB localStorage 误报
    - **C 手牌展开**：Frequency Explorer 点击行动线行展开具体手牌列表（日期/类型/盈亏）
- **V7.4.3** — Frequency Explorer：按场景+牌面+行动线分组频率
    - **新卡片**：Hand 面板 Hand History 上方新增 `🔍 Frequency Explorer`（有 boardCode 数据时显示）
    - **场景检测**：`ggParser._detectScenario()` 自动匹配 BTNvsBB / SBvsBB / COvsBTN
    - **分组统计**：按场景 + 牌面分类筛选 → 行动线频率 + 平均盈亏 + 手数
    - **筛选器**：场景 + 牌面分类双下拉联动
- **V7.4.2** — 手牌字段迁移：boardCode / boardCategory 写入 HandReview
    - **ggImport 写入**：3 处手牌创建点写入 `boardCode` / `boardCategory`
    - **数据迁移**：`_migrateBoardFields()` 启动时扫描旧手牌，基于 `boardCards` 补字段
    - **迁移标记**：`pa_migrated_board_v1` 一次性执行，不重复扫描
- **V7.4.1** — GTO 频率判断训练器 + 牌面规范化
    - **Quiz Trainer**：`src/modules/quizTrainer.js`，从 gtoRaw 随机抽题，用户选择下注尺度 → 对比 GTO 频率 → "合理/偏差"判定 + 频率分布图；支持按场景（SBvsBB/BTNvsBB）和 7 阶段切换；进度持久化 localStorage
    - **牌面规范化**：`ggParser._normalizeBoardCode()` 输出 gtoRaw 格式编码；`_classifyBoardCategory()` 7 分类标签
    - **UI**：Odds 面板 SRP 表下方新增 `🎯 GTO Quiz` 折叠卡片
- **V7.4.0** — 策略知识库重构：gtoRaw 统一管理 + gtoBaseline 自动编译
    - **gtoRaw JSON**：新建 `src/data/strategy/gtoRaw/BTNvsBB_SRP_flop.json`（183 boards）+ `SBvsBB_SRP_flop.json`（184 boards），来自 GTO Solver 原始输出
    - **gtoBaseline.js**：从 gtoRaw 自动编译极端阈值（>90% / <5% 频率），`detectExtremes(hand)` 检测 L1 偏差
    - **架构统一**：`gtoRaw/` 为唯一数据源，`srpData.js` 和 `gtoBaseline.js` 为派生视图
    - **目录结构**：`src/data/strategy/gtoRaw/` + `gtoBaseline.js`

## V7.3

- **V7.3.4** — Neon Deck 第四配色方案 + Tilt 遮罩关闭 Bug 修复
    - **Neon Deck 配色**：深绿底微弱点阵网格 + 卡片绿色辉光边框 + blur 毛玻璃 + 松石绿导航 glow；标题 ♠♥ 花色装饰 + stats 顶边辉光线 + canvas 辉光滤镜
    - **切换循环**：Pale → Nimbus → Ember → **Neon** → Pale，四方案 CSS class 统一管理
    - **Bug 修复**：点击遮罩关闭 tilt overlay 时同步清 `locked` + 停计时器
- **V7.3.3** — PC 端加宽 + 单面板模式
    - **CSS 桌面断点简化**：删除 `.main` 的 Timer/Odds `grid-template-columns` 双栏规则，改回一个 Tab 只显示对应面板
    - **`.app` max-width**：1200px → 1400px
    - **Odds 内部比例调整**：计算器占 2/3（`grid-template-columns: 2fr 1fr`）
    - **JS 简化**：`bindNav()` 删除 timer||odds 双面板显示分支，始终只激活当前 Tab 的面板
    - **效果**：桌面端三 Tab 各占全宽，Timers/Odds 不再强制合屏

- **V7.3.2** — Session 面板加手牌累计盈利图 + 悬停 tooltip + Session 间隔调整
    - **新方法** `_renderSessionHandChart(sessionId)`：过滤单 Session 手牌 → 累计 `pBB` → Canvas 单线图
    - **Session 图 hover tooltip**：canvas 存储 `_hitPoints`，mousemove 显示手牌序号/日期/水后 profit
    - **SESSION_GAP_HOURS**：3h → 1h，GG 导入时手牌间隔 >1h 即分新 Session；线下 Live 手动创建不受影响

- **V7.3.1** — 图表 hover tooltip 交互 + crosshair
    - **Tooltip DOM 层**：固定定位 `<div id="chartTooltip">`（`pointer-events: none`），canvas 旁显示悬浮数据卡片
    - **累计盈利图**：hover 最近数据点 → 显示手牌序号/日期/profit/profit w/o rake；vertical crosshair 虚线（快照恢复 + 增量绘制）
    - **Session 柱状图**：hover 柱子 → 显示日期/手数/Session盈亏/累计盈亏
    - **位置柱状图**：hover 柱子 → 显示位置名/手数/盈亏
    - **Hit-test 数据**：每张图表渲染后在 `canvas._hitPoints` / `canvas._hitBars` 存储屏幕坐标数据
    - **Crosshair**：累计图首次渲染后 `getImageData` 快照，mousemove 时 `putImageData` 恢复 → 画竖虚线（零开销重绘）
    - **事件委托**：`#chartsArea` 上单次绑定 `mousemove` + `mouseleave`，`_tooltipBound` 防重复

- **V7.3.0** — 图表改版：累计盈利 + Session 柱状叠加 + 位置盈亏
    - **图表 1 — 累计盈利线图**（全新）：全手牌累计盈亏走势，双线（profit 红/profit w/o rake 青），半透明填充，>1000 手自动采样防卡顿，X 轴日期标签
    - **图表 2 — Session 柱状 + 累计叠加**（改版）：原折线图改为 25 场柱状图（正绿负红），金色累计折线叠加，光晕效果
    - **图表 3 — 位置盈亏柱状图**（全新）：EP/MP/CO/BTN/SB/BB 六位置 profit 柱状，正绿负红，数据来自手牌 hero position 聚合
    - **_aggregatePositionProfit()**（新函数）：从 HandRepo 全量手牌按位置分组累加 pBB
    - **renderCharts()**（新函数）：RAF 防抖，统一调度 3 张图表渲染；替换旧 `renderChart`/`_doRenderChart`
    - **HTML/CSS**：canvas 替换为 `#chartsArea` 容器，`charts-area` flex column 布局，`chart-title` 小标题，canvas CSS 统一样式
    - **ResizeObserver**：app.js 适配 `chartsArea` + `renderCharts()`，不再引用旧 `profitChart` canvas

## V7.2

- **V7.2.5** — SRP 筛选器紧凑化
    - **四选一排列**：SB v BB SRP 四个筛选下拉框改为 CSS Grid 四等分单行排列（`grid-template-columns: repeat(4,1fr)`），≤420px 窄屏自动降为两两排列
    - **字号缩小**：筛选器 `font-size:0.7em`、`padding:5px 4px`，与表格字号一致
- **V7.2.4** — 分享按钮迁移 + 图表重写
    - **分享网站**：从数据迁移菜单移至设置菜单（🌐 分享），位于配色方案旁
    - **图表重写**：柱状图→折线+面积图，Nimbus 配色（#0f0f0f 底色/微网格/盈绿亏红渐变填充）；数据点 3px 圆 + 轮廓；趋势线金色 MA3 虚线 + 光晕；圆角 clip
    - **e2e 修复**：移除已删除的 Tournament Tab 测试
- **V7.2.3** — PaleUI 三配色方案
    - **Pale（默认）**：PaleUI 暗色模式 — 纯黑底 `#0f0f0f`、卡片与底色同色、白透明边框 `rgba(255,255,255,0.06-0.12)`、微阴影 `shadow-sm`、`:focus-visible` 聚焦环、按钮 36px 统一高度 + inset hover
    - **Nimbus**（原 V7）：蓝黑底 `#141b24`、卡片 `#1e2839`、松石绿点缀、class 切换名 `body.color-nimbus`
    - **Ember**（原 V11）：纯黑底、卡片 `#1a1a1a` 微亮、松石绿顶边、class 切换名 `body.color-ember`
    - **三方案循环**：设置齿轮 → 🎨 配色按钮循环切换 Pale → Nimbus → Ember，持久化 `pa_colorScheme`
- **V7.2.2** — /simplify 代码审查与清理
    - **db.js 复用 DB.count()**：putAll 内联事务验证改用已有的 `DB.count()` 方法，消除重复代码
    - **db.js DEV 门控**：写入后 count 校验加 `CONSTANTS.DEV` 门控，生产环境跳过额外 IndexedDB 事务
    - **store.js 提取 _mergeDictImport()**：三个对手数据字典合并块（aliases/liveFlags/merges）提取为公共辅助函数（11 行 → 3 行）
    - **store.js 修复副作用**：opponentLiveFlags/Merges 导入合并改为先复制再赋值，与 opponentAliases 一致，避免原地修改缓存对象
    - **store.js 迁移时序修复**：`_migrateOpponentHash` 先 `HandRepo._flush()` 再设置迁移标记，防止 300ms 写入窗口期崩溃导致数据丢失
    - **review.js 渲染优化**：`SessionRepo.getAll()` 移到渲染指纹检查之后，无变更时不再获取 Session 数据
    - **statsEngine.js**：删除重复的注释行
    - **main.js**：致命错误回退 UI 配色同步新设计系统（`#0f172a`→`#141b24`）
    - **注释清理**：app.js / odds.js 部分注释精简为纯版本标记

- **V7.2.1** — 双配色方案切换
    - **配色切换按钮**：设置菜单新增 `🎨 V11` 按钮，一键在 V7（蓝黑/松石绿）和 V11（纯黑/松石绿+蓝统计）之间切换
    - **V11 配色**：纯黑底 `#0f0f0f`、中性灰卡片 `#1a1a1a`、松石绿顶边微装饰、蓝统计 `#0585BE`、GTO Wizard 风格花色红 `#ED2543`、警告橙 `#F9720C`
    - **持久化**：`localStorage.pa_colorScheme` 保存选择，刷新不丢失
    - **数据色共享**：松石绿、盈利绿、亏损红、金色在两套方案中不变，仅结构色切换

- **V7.2.0** — 全局设计系统重构（CodePen 风格 × 专业扑克工具）
    - **配色**：底色 #141b24（蓝黑）、卡片 #1e2839（带蓝调）、主色 #5a9e8f（松石绿）、强调金 #d4a853、盈亏绿 #6baf7e / 盈亏红 #c06060；全局 ~20 色值从暖橙体系切换到冷蓝绿体系
    - **圆角柔化**：卡片 16→12px、按钮 12→8px、输入框 8px、导航 10→8px；整体从"锐利"转向"柔和"
    - **卡片层次**：去阴影，换 1px `rgba(255,255,255,.04)` 微亮边 + 提亮卡片与底色拉开 ~7% 亮度差
    - **字体升级**：正文 "Segoe UI Variable Text" / "SF Pro Text"、数据等宽 "SF Mono" / "Cascadia Code" / "JetBrains Mono"、基准 15px / 行高 1.5
    - **花色颜色**：`.suit-red` #e0556a（♥♦）、`.suit-black` #e8e8ec（♠♣）
    - **导航重写**：微渐变背景 + 细边框、激活态松石绿填充 + 辉光阴影、hover 微提亮、去掉上浮动效
    - **统计面板增强**：卡片斜角微渐变 + hover 提亮、选中行松石绿左边框
    - **全局 transition**：150ms→120ms ease-out
    - **内联颜色同步**：review.js / handPicker.js / ggImport.js / odds.js 中硬编码色值全量替换

## V7.1

- **V7.1.0** — 场景驱动的响应式布局重构
    - **三 Tab 导航**：Tournament 从主 Tab 移除，迁入 Odds 底部折叠（`<details>`）；导航精简为 Timer / Odds / Review
    - **响应式断点**：`max-width: 480px` → `960px`；`<main data-active-tab="X">` 驱动布局模式；移动端（<768px）单面板，桌面端（≥768px）双栏
    - **Timer + Odds 同屏**：桌面端打牌模式下两面板 CSS Grid 并排
    - **Review 桌面双栏**：子面板卡片 CSS Grid 两列自动排列（Session 表单‖统计、Hand 表单+Picks‖列表、Villain 列表限高 70vh）
    - **Odds 桌面增强**：Odds Tab 激活时计算器 + SRP 策略表内部双栏；Sizing Cheatsheet / Implied Odds 桌面端默认展开
    - **Outs → Equity 速查表**：0-20 outs 对应 Flop/Turn 胜率，当前 outs 值橙色高亮，随输入实时刷新
    - **SRP 全表英文化**：Summary / 表头 / 筛选选项 / 数据值（`srpData.js`）全部英文；`app.js` 清理 Tournament import/init

## V7.0

- **V7.0.3** — 复审修复：Flop 解析 + putAll 校验 + 迁移原子性
    - **_parseFlop 对齐**：改为 `i % 2 === 0` 遍历模式（与 Turn/River 一致），翻牌 >3 个 action 时不漏英雄行动
    - **putAll count 校验**：DB 写入后校验记录数，不匹配时 console.warn 报警
    - **迁移标记原子性**：`_dbReady = true` 先于 `pa_migrated_v1`，避免中间崩溃造成标记为真但 repo 未 ready
- **V7.0.2** — 对手画像数据模型修复 + 文档同步
    - **opponentHash**：对手唯一标识 — `Utils.normalizeOpponentName()`（去空格+统一小写）；ggParser 解析时同步生成 `oHash`；ggImport 导入时写入
    - **getOpponentStats 改用 oHash 分组**：同一对手大小写变体自动聚合；返回对象增加 `oHash`/`oIds` 字段
    - **数据迁移**：`_migrateOpponentHash()` 启动时扫描旧手牌补 oHash
    - **对手合并存储**：`Store.opponentMerges`（`{ canonicalHash: [oId1, ...] }`），导出/导入同步
    - **ARCHITECTURE.md 重写**：同步到 V7.0.2（22 模块、V7.0 性能优化、Store Schema 完整字段）
    - **getOpponentStats 缓存**：排序/筛选/Live 切换时手牌总数未变则跳过全量聚合（`_oppStatsCache`）
    - **Session Map O(1)**：`sessionsMap.get()` 替代 `sessions.find()`，每手牌查找从 O(n) → O(1)
    - **对手统计懒加载**：`analyze()` 从渲染时全量预计算 → 展开时才计算一次，`data-rendered` 防重复
    - **analyze() 指纹缓存**：首尾手牌 ID + 数量 + 过滤条件为 key，跨面板（Overall/Session/Weekly/Villain）共享结果
    - **缓存自动失效**：`store.js` 的 `HandRepo.saveAll()` 自动调用 `clearStatsCache()`，无需手动清缓存
    - **渲染指纹**：Villain 面板（对手数+交手数+排序+筛选）和 Overall 面板（Session数+手牌数）数据未变则跳过 DOM 重建
    - **手牌行懒渲染**：对手手牌列表从初始全量 HTML 生成 → 展开时才通过 `_buildOppHandListHtml()` 构建
    - **事件委托统一**：Villain 面板 5 组 `querySelectorAll().forEach` 独立绑定 → `#opponentList` 单一 `addEventListener`，handler 引用存储防重复叠加
    - **效果**：5000 手牌/50 对手场景下，Villain 面板打开 ~3000ms→~30ms，切 Tab ~200ms→~0ms，排序切换 ~3000ms→~30ms
- **V7.0.1** — 统计指标参考范围修正 + Recommendations 改造
    - **STAT_RANGES 修正（13 项）**：4-Bet[8,13]→[4,8]、Squeeze 上调至[10,16]、Steal≥40→≥45、C-Bet Flop/Turn/River 逐街递减、AFQ Flop/Turn/River 逐街递增、Donk Bet acceptable 收紧、Bet vs Missed CBet warn 改为[0,10]（修复边界 Bug）、新增 Check-Raise Flop[8,15]/Probe Bet Turn[35,55]
    - **RECOMMENDATION_RULES 修正**：7 条阈值随范围同步修正，新增 13 条规则（Squeeze 过高/4-Bet 过高/C-Bet Turn 过高/C-Bet River 过高+过低/AFQ 三街 过高+过低/Check-Raise 过低/WTSD 过高）
    - **RECOMMENDATION_TEXTS 全部重写**：指示问题 → 解释 WHY → 给出具体行动方案，新增 AFQ/Check-Raise/Probe Bet 等 6 条文案
- **V7.0.0** — 性能优化：缓存层 + 懒加载 + 渲染指纹
## V6.19

- **V6.19.9** — 代码结构收尾
    - **setSafeHTML 统一**：`review.js` 内 `_setSafeHTML` 提升为 `Utils.setSafeHTML()`，全模块可复用
    - **importAll 防御**：`data.logs` 增加 `typeof === 'object'` 类型检查
- **V6.19.8** — 事件委托去重 + 统计引擎缓存
    - **事件委托去重**：删除 `renderHandReviews()` 中 2 处 forEach 重复绑定（expand/mark 按钮），`#handBody` 委托已覆盖
    - **统计缓存**：`statsEngine.js` 新增 `_contextCache`（Map），`createHandContext()` 结果按 `hand.id` 缓存；新增 `clearStatsCache()` 导出
    - **效果**：对手画像面板降为 O(n) 而非 O(n×m)，手牌重复解析消除
- **V6.19.7** — UX + 性能微优化
    - **AudioContext 复用**：`beep()` 缓存单个 AudioContext 实例，不再每次创建
    - **escapeHtml 优化**：复用单个 div 元素，不再每次 `document.createElement`
- **V6.19.6** — 图表渲染防抖
    - **renderChart**：`requestAnimationFrame` 防抖，避免 `renderAll` → `updateTotalStats` → `ResizeObserver` 同帧 2-3 次绘制；`_doRenderChart` 内部分离实际绘制逻辑
- **V6.19.5** — 盈亏格式化统一
    - **review.js**：2 处独立盈亏格式化 `'<span style=...>' + sign + safeFixed + ' BB</span>'` → `Utils.formatProfitHTML()`
    - **效果**：绿色/红色颜色、± 符号、safeFixed 精度统一收口
- **V6.19.4** — 卡牌渲染统一（消除重复代码）
    - **review.js**：3 处独立卡牌渲染循环 → `Utils.renderCardBadges()`（共减少 ~24 行）
    - **handPicker.js**：1 处 → `Utils.renderCardBadges()`
    - **效果**：花色映射、红黑判断、badge HTML 生成逻辑统一收口到 utils.js
- **V6.19.3** — Toast 通知替代 alert（28 处）
    - **ggImport.js**：11 处 `alert()` → `Utils.showToast()`，导入反馈不再阻塞 UI
    - **review.js**：9 处替换（1 处 `location.reload` 前保留 alert 确保执行）
    - **dataSync.js**：7 处替换
    - **tiltRescue.js**：1 处替换
- **V6.19.2** — 代码审查修复：Bug 修复 + 重复提取 + 死代码清理 + Toast
    - **Bug 修复**：`sortByDateKey` 改为 `slice().sort()` 避免污染 BaseRepo 内部缓存；4 处空 catch 块加 `console.warn`
    - **代码提取**：`Utils.renderCardBadges()` 统一卡牌徽章渲染；`Utils.formatProfitHTML()` 统一盈亏带颜色格式化
    - **死代码清理**：删除 `_tiltLogSavedBound` 无效变量；删除 `_renderHealthIndicator` 死调用；test notify 的 `console.log` 加 `CONSTANTS.DEV` 守卫
    - **Toast 通知**：`Utils.showToast()` + CSS 动画弹窗，替代 `alert()` 阻塞式弹窗（基础设施就位，逐步迁移 34 处调用）
- **V6.19.1** — Opponent Profiles 交互修复 + 手牌筛选 + 信息重组
    - **点击修复**：对手标题行点击 toggle 展开，详情内部点击不再关闭面板
    - **手牌筛选**：对手手牌列表新增 [全部] [≥2 BB] [≥30 BB] 筛选按钮，过滤后仅显示符合条件的手牌
    - **信息重组**：手牌行首列从 type+board 改为 Hero 底牌花色徽章；展开详情 "牌面" 改为 "底池类型" 仅显示 potType；删除对手底牌列表
    - **按钮重命名**：对手面板 "✎ 编辑手牌" → "👁️ 查看手局"，Session 展开 "✎" → "👁️"

- **V6.19.0** — Bug 修复 + Live Data 布局调整
    - **Bug 修复**：`renderOpponentProfiles` 中 `profiles` 变量名错误导致 Villain 面板崩溃，改为正确的 `stats`
    - **Live Data 下移**：Live Data 卡片从 Villain 顶部移到 Opponent Profiles 下方

## V6.18

- **V6.18.9** — Position 位置统计 + Villain 对手画像增强 + 玩家数据迁移
    - **Position 位置统计**：Overall 面板新增 `▶ Position` 折叠区，EP/MP/CO/BTN/SB/BB 六个标准位置各展示 VPIP/PFR/3-Bet/C-Bet/WTSD/WWSF，仅显示有数据的位置
    - **Villain 对手画像增强**：展开对手后新增 `▶ Opponent Stats` 折叠区，展示该对手的完整引擎统计（Overview/Preflop/Postflop），至少 3 手牌才渲染
    - **玩家数据迁移**：Live Data 卡片从 Total 面板移到 Villain 面板顶部
    - **Weekly Tooltip 修复**：Weekly Stats 统计卡片现在可点击显示说明

- **V6.18.8** — 白金极简 Cyber 标题风格
    - 分区标题改为方案 C：浅灰白 #e2e8f0、细体 400、宽字距 0.18em、弱白色辉光、淡灰细线边框

- **V6.18.7** — 折叠行为修正 + Total→Overall + 赛博朋克标题
    - **折叠修正**：Preflop/Postflop 父面板默认折叠，展开后子组全部展开（之前行为相反）
    - **Total→Overall**：子 Tab 按钮和面板标题改为 "Overall"
    - **赛博朋克标题**：`.stats-section-title` 改为居中、上下霓虹边框、文字发光（`text-shadow: 0 0 8px`）、渐变背景微光

- **V6.18.6** — Preflop/Postflop 默认展开 + Session 统计入口 + 手牌编辑跳转
    - **Preflop/Postflop 默认展开**：父面板 `<details>` 增加 `open` 属性，与 Overview 一致默认可见，子组仍可折叠
    - **Session 统计入口**：点击 Session 行展开按钮（📋）自动在 `#statsArea` 渲染该场引擎统计
    - **Session 手牌编辑跳转**：Session 展开区手牌行的 ✎ 按钮自动切换到 Hand 子 Tab 并触发 `editHandReview`，之前该按钮因缺少事件委托完全无效

- **V6.18.5** — Weekly 对接 statsEngine + 可复用面板落地
    - **renderStatsPanel**：提取公共引擎统计渲染函数，Total/Session/Weekly 三面板共用（水钱行 → Overview → Preflop → Postflop → Recommendations）
    - **Weekly 引擎统计**：本周自动统计下方新增 `▶ Weekly Stats` 折叠区，展示该周的 Preflop/Postflop 引擎指标
    - **代码精简**：`updateTotalStats` 和 `updateStatsForSession` 引擎统计部分从 ~40 行重复代码缩减为 1 行 `renderStatsPanel()` 调用
    - **数据流**：Total = 全部手牌，Session = 选中 Session 的手牌，Weekly = 本周所有 Session 的手牌。Weekly 不新增 dateFrom/dateTo 过滤，由调用方预过滤 hands 数组传入 analyze()

- **V6.18.4** — Preflop/Postflop 嵌套分组 + 统计点击说明 Tooltip
    - **Preflop 三子组**：父面板 Preflop 下嵌套 Preflop Open（VPIP/PFR/Limp） / Vs. Raise（3-Bet/4-Bet/Fold to 3-Bet/Fold to 4-Bet/Squeeze/Cold Call）/ Steal Dynamics（Steal/Fold to Steal）
    - **Postflop 收归父面板**：Postflop 父面板下嵌套 Preflop Aggressor / Preflop Defender / Showdown Stats / Aggression by Street
    - **Tooltip 点击说明**：点击任意统计卡片弹出模态弹窗，显示指标名称和中文说明（33 个指标均有）；点击外部或 Close 按钮关闭
    - **STAT_TOOLTIPS**：在 statsEngine.js 中为每个指标定义说明文案（借鉴 GGPoker-Hand-Analyzer 的 tooltip 系统）
    - **_renderNestedGroup**：模块级辅助函数支持单父多子的嵌套 `<details>` 结构

- **V6.18.3** — 翻后四子组拆分 + 补全 3 指标 + 可复用面板
    - **翻后四子组**：Postflop 拆为 Preflop Aggressor / Preflop Defender / Showdown Stats / Aggression by Street
    - **新增 3 指标**：Fold to 4-Bet（Hero 3-bet 后被 4-bet 弃牌率）、Check-Raise Flop（翻前跟注者翻牌过牌-加注率）、Probe Bet Turn（翻前跟注者翻牌全过后转牌试探下注率）
    - **可复用渲染**：`_renderStatSection()` 模块级辅助函数，Total 和 Session 两面板共用同一渲染逻辑
    - **UI 英文化**：基础统计标签改为 Sessions / Net Profit / Hands / Hourly / Avg Tilt；分区标题英文（Overview / Preflop / Preflop Aggressor 等）
    - **preflopKeys 扩展**：Session 面板翻前指标从 4 个扩展到与 Total 一致的 11 个

- **V6.18.2** — 0% 统计指标修复 + oId 过滤预留
    - **Bug 修复（类别 1）**：12 处翻后统计令牌格式比较错误 — cbetFlop/cbetTurn/cbetRiver/foldToCbet/raiseCbetFlop/donkBetFlop/betVsMissedCbet/wtsdAfterCbet 的 process 函数将令牌 `"B60"` 与字符串 `'bets'` 比较，改为首字符匹配（`[0] === 'B'`/`'F'`/`'R'`）
    - **Bug 修复（类别 2）**：foldTo3bet 自相矛盾 — heroAction 是单一字符串无法同时为 `'raises'` 和 `'folds'`；新增 `heroWasRaiser` 标志跟踪 Hero 是否曾加注，faced3Bet 检测改用该标志
    - **Bug 修复（类别 3）**：reachedShowdown 正则不匹配 — GG 解析器输出 `shows [cards]` 无 "Hero" 前缀，正则改为 `/\bshows \[/i`
    - **可复用面板预留**：`calculateStats()` 新增 `opts.oId` 对手 ID 过滤，为对手画像面板复用统计引擎做准备
    - **子 Tab 重排**：Hand → Session → Weekly → Total → Villain

- **V6.18.1** — 三向补全 + Total 子 Tab 拆分
    - **getStatColor 单值阈值**：支持单元素数组表示单向阈值（如 `[40]` 表示 ≥40 好），对齐参考项目 ranges 语义
    - **fold_to_steal 新增**：Hero 在盲位面对 CO/BTN 偷盲弃牌率，增强 `_parsePreflop` 提取对手位置
    - **Session 衍生统计**：新增 手/小时、BB/小时 两个全局衍生指标，显示在 Total 总览区
    - **Total 子 Tab**：新建 Total 面板（5 子 Tab 首位），全局统计从 Session 面板平移；Session 面板改为本场统计视图（点 Picks Session 触发）
    - **STAT_RANGES 修正**：squeeze/stealAttempt/wtsdWon/wwsf 改用参考项目的单值阈值
    - **优化建议扩展**：新增 Fold to Steal 规则

- **V6.18.0** — 统计指标对齐 GGPoker-Hand-Analyzer（15 → 30 指标）
    - **createHandContext 增强**：新增 Hero 位置提取、turn/river 街道解析、faced3Bet 检测、reachedShowdown/isHeroPreflopCaller 等派生标记
    - **翻前新增 5 指标**：Limp（平跟入池）、Cold Call（冷跟注）、4-Bet、Steal（偷盲）、Fold to 3-Bet
    - **翻后新增 5 指标**：C-Bet 转牌、C-Bet 河牌、Raise CBet、Donk Bet、攻击错失 CBet
    - **摊牌新增 4 指标**：WTSD 胜率、CBet 后进摊牌率、WWSF（攻击者）、WWSF（跟注者）
    - **攻击性新增 3 指标**：AFQ 翻牌、AFQ 转牌、AFQ 河牌
    - **优化建议扩展**：从 9 条 → 18 条（新增 Limp/Cold Call/4-Bet/Steal/FoldTo3bet/CBetTurn/Donk/WTSD胜率/WWSF 建议）
    - **finalizeStats 增强**：支持 AFQ 型统计（bets/raises/calls/checks → 百分比）

## V6.17

- **V6.17.3** — 统计面板分类重组：总览 / 翻前 / 翻后（折叠面板）
    - **折叠面板**：三个分区改为 `<details>` 折叠面板（总览默认展开，翻前/翻后默认折叠），点击展开/收起，▶ 箭头旋转动画
    - **总览区**：引擎计算的总手数 / bb/100扣水后 / 扣水前总盈利 / bb/100扣水前
    - **翻前区**：VPIP / PFR / 3-Bet / Squeeze（颜色编码）
    - **翻后区**：C-Bet 翻牌 / Fold to CBet / WTSD / WWSF（颜色编码）
    - **样式**：新增 `.stats-details`、`.stats-section-title`（橙色 summary 标题 + 底部分隔线）

- **V6.17.2** — 统计引擎深化：createHandContext + 统计颜色编码
    - **createHandContext**：新增 `createHandContext(hand)` 预计算函数，每手牌只解析一次翻前/翻后信息，所有 STAT_DEFINITIONS 的 `process()` 共享上下文，消除重复解析（原 12 个指标 × 2 次解析/手 → 2 次/手）
    - **统计颜色编码**：新增 `getStatColor(value, statKey)` 导出函数，基于 STAT_RANGES 理想范围返回绿/黄/红 CSS 类；新增 `PERCENT_STAT_KEYS` 导出，定义技术统计展示顺序
    - **技术统计行**：Session 统计面板新增一行技术统计（VPIP/PFR/3-Bet/Squeeze/C-Bet 翻牌/Fold to CBet/WTSD/WWSF），每个指标按理想范围着色（绿=理想/黄=可接受/红=偏离）
    - **样式**：新增 `.stat-good`（绿 #22c55e）、`.stat-acceptable`（黄 #f59e0b）、`.stat-warn`（红 #ef4444）

- **V6.17.1** — 生产构建 DEV 剔除
    - **Vite 插件**：`vite.config.js` 新增 `production-dev-flag` 插件，构建时将 `src/constants.js` 的 `DEV: true` 自动替换为 `DEV: false`
    - **体积缩减**：212KB → 202KB（−10KB），selfTests（363 行）完全从生产构建剔除，gzip −3.4KB
    - **效果**：生产环境不再运行自测代码和 DEV 预检查（`Store._setRaw` 中的 `console.assert` 等）

- **V6.17.0** — 代码结构优化（零功能变更）
    - **store 拆分**：`DB` IndexedDB 封装提取到独立 `src/store/db.js`；`store.js` 627→527 行
    - **innerHTML 消除**：`review.js` 3 处关键用户数据路径改为 `createContextualFragment` + `replaceChildren`
    - **ARCHITECTURE.md 重写**：同步到 V6.17.0（18 模块、补 statsEngine/handPicker/db.js、修正 Store Schema、双托管平台）
    - **文件结构**：22 个 ES 模块，6292 行

## V6.16

- **V6.16.5** — Hand History 折叠 + Picks 跳转增强 + Session 统计过滤
    - **Hand History 默认折叠**：改为 `<details>` 折叠组件，默认关闭，点击标题展开；从 Picks 跳转时自动展开
    - **Picks → 手牌跳转增强**：展开详情后自动触发编辑（`editHandReview`），表单填充 hand notes / 反思等信息
    - **Picks → Session 跳转增强**：跳转后统计面板自动过滤为仅该 Session 数据，标题显示 Session 名称 + "↺ 全部"按钮一键恢复全局统计
    - **新增 `updateStatsForSession()`**：按 sessionId 过滤展示单场统计 + 水钱 + 优化建议，支持一键切回全部 Session

- **V6.16.0** — Picks 移入 Hand 面板 + 点击跳转
    - **Picks 位置调整**：从独立子 Tab 移入 Hand 面板内，位于 Hand History 上方；无精选时整个卡片隐藏，有精选时显示
    - **子 Tab 精简**：复盘导航从 5 个恢复为 4 个（Hand / Session / Weekly / Villain）
    - **点击跳转手牌**：Picks 行点击跳转到 Hand History 对应手牌并自动展开详情，滚动到视口中央
    - **点击跳转 Session**：Session 列显示为橙色下划线链接，点击切换到 Session 子 Tab 并展开对应 Session

## V6.15

- **V6.15.3** — 行点击选中替代 checkbox
    - **无 checkbox 交互**：移除手牌行尾的 checkbox，改为点击行任意位置（排除按钮）切换选中/取消
    - **选中视觉反馈**：选中行左侧橙色边框条 + 背景微亮（`.is-selected`），点击即刻生效
    - **批量栏按钮化**：`Select All` 按钮替代全选 checkbox，全部选中时自动切换为 `Deselect All`
    - **状态管理**：`Review._selectedHandIds`（Set）驱动选中状态，`_updateHandBatchUI()` 同步按钮文字、计数、行样式
    - **文案更新**：批量栏 `已选 N 项`→`N selected`、`删除选中`→`Delete`、`关联`→`Link`、`关联到 Session`→`Link to Session`

- **V6.15.2** — Session 展开区字段调整 + 批量文件导入
    - **Session 展开区字段调整**：移除 `potType + board`，改为显示 Hero 底牌花色徽章（♠♥♦♣）；如有对手牌信息，灰色小字标注 `vs Xx Xx` 区分
    - **批量文件导入**：拖拽支持多文件，遍历读取所有 .txt 文件后拼接内容统一解析；单文件读取失败 alert 跳过不影响其他；非 .txt/.json 文件给出格式提示
    - **拖放区文案**：更新为"拖拽 .txt 文件到此处（支持多文件）"

- **V6.15.1** — 手牌筛选菜单 + 折叠式展示
    - **手牌筛选栏**：Hand History 表格上方新增筛选栏，支持按底池大小筛选（全部 / ≥2BB / ≥30BB 关键手牌）、按 Session 筛选（全部 / 未分类 / 具体 Session）；筛选结果实时计数
    - **手牌折叠展开**：每行新增 ▶ 按钮，点击展开显示完整手牌详情（街道行动、错误类型、反思、操作按钮）；手牌 Body 事件委托覆盖展开行内的编辑/删除/标记操作
    - **Session 展开区筛选**：Session 展开的手牌列表新增底池大小筛选下拉框，change 事件自动重新渲染筛选结果
    - **toggleSessionExpand 增强**：支持 `filterVal` 参数，筛选切换时移除并重建展开行

- **V6.15.0** — 手牌精选模块 + 三层手牌管理层
    - **手牌标记**：HandReview 新增 `marked` 字段（旧数据自动补 `false`）；手局列表每行 + Session 展开行均增加 ☆/★ 标记按钮，点击切换标记状态，★ 为金色高亮
    - **Picks 子面板**：复盘新增第五个子 Tab "Picks"（手牌精选），独立展示所有已标记手牌，包含时间/Session/底牌/盈亏/错误类型/取消标记操作
    - **新建 handPicker.js**：`HandPicker` 模块管理标记手牌的展示、取消标记，提供 `getMarkedHands()` 接口供统计引擎调用
    - **事件委托**：sessionBody 增加 `data-hand-mark` 事件委托，Session 展开区域的标记按钮即时生效
    - **样式**：`.hand-mark-btn` hover 放大 1.2x

## V6.14

- **V6.14.0** — 导入流程重构：GG 导入移到 Session + 文件拖拽
    - **GG 导入入口迁移**：从手牌面板移到 Session 面板；Session 列表顶部新增"📥 导入牌谱"快捷按钮；每个 Session 行新增 📥 按钮可导入牌谱到指定 Session
    - **Session 自动分组**：快速导入无指定 Session 时，按 3 小时间隔自动分拆手牌，创建新 Session 或匹配合并到已有 Session
    - **文件拖拽导入**：GG 导入覆盖层新增拖放区，支持拖拽 .txt 牌谱文件或点击选择文件，自动读取并解析
    - **向后兼容**：旧手牌（`sessionId=null`）保留不动，不自动分配；手牌面板批量操作可手动关联到 Session
    - **统计引擎扩展**：`calculateStats()` 新增 `sessionId` 过滤参数，为后续按 Session 细化分析打基础
    - **样式**：拖放区 hover 橙色高亮边框（`.gg-drop-active`）

## V6.13

- **V6.13.0** — 统计引擎重构 + 水钱/Jackpot + 优化建议
    - **统计引擎**：新建 `src/modules/statsEngine.js`，借鉴 GGPoker-Hand-Analyzer 的声明式 STAT_DEFINITIONS 架构，每个指标独立定义（`init` + `process`），核心引擎遍历手牌调用定义
    - **水钱/Jackpot 解析**：`ggParser.js` 新增 `rake` 和 `jackpot` 提取，从 SUMMARY 行解析 `Total pot $X | Rake $Y | Jackpot $Z`；`ggImport.js` 导入映射同步写入
    - **水钱影响展示**：Session 子 Tab 统计面板新增"总水钱""总 Jackpot""扣水前总盈利""bb/100（扣水前）"四项指标
    - **专属优化建议**：Session 子 Tab 统计面板下方展示，基于 VPIP/PFR/3Bet/Squeeze/CBet/WTSD 等指标阈值自动生成改进建议
    - **高级统计指标**：首期引入 VPIP、PFR、3-Bet、Squeeze、C-Bet 翻牌、Fold to CBet、WTSD、WWSF 共 8 项
    - **数据迁移**：`store.js` 旧 HandReview 自动补 `rake: 0, jackpot: 0`
    - **样式**：新增 `.rec-card` 建议卡片（橙色左边框）和 `.rec-good` 绿色无建议状态

## V6.12

- **V6.12.5** — Tab 按钮 hover 动效
    - **主 Tab 按钮**：hover 上浮 1px + 阴影 + 颜色变亮；激活态 hover 橙色辉光增强
    - **子 Tab 按钮**：同上逻辑
    - **过渡动画**：`transition: all 0.15s ease` 统一手感
- **V6.12.4** — 版本号修复 + PWA 通知增强 + CLAUDE.md 更新
    - **版本号统一**：`src/constants.js` 从 6.10.1 → 6.12.4，`package.json` 从 6.12.3 → 6.12.4，CLAUDE.md 同步
    - **PWA 通知增强**：通知走 Service Worker (`reg.showNotification`)，支持 `notificationclick` 点击回到 App；`requireInteraction: true` 通知不自动消失；添加"测试通知"按钮
    - **SW 增强**：`public/sw.js` 和内联 Blob SW 均添加 `notificationclick` 处理
    - **CLAUDE.md 更新**：文件结构补充 `ggImport.js`、`e2e/`、`docs/`；UI 导航更新为四子 Tab
- **V6.12.3** — Live 筛选 + 决策/反思合并 + 行动线缩小
  - **Live 全局筛选**：对手列表排序栏加 `LIVE` 筛选按钮，点击只显示已标记 Live 的对手，再点恢复全部。删除每行的独立 Live 切换按钮。
  - **决策/反思合并**：删除"决策描述"(decision) 输入框，"结果与反思"(reflection) textarea 移到原位置，rows=4。保存/编辑手牌时不再读写 decision 字段。
  - **行动线速查缩小**：从独立卡片移到思考模板下方，去掉卡片边框，字体缩小，默认折叠。

- **V6.12.2** — 对手画像文案英文化
  - **排序按钮**：`最近↓`→`Recent`、`盈亏↓`→`Net`、`胜率↓`→`Win Rate`，去掉 ↓ 箭头
  - **Live 标记**：🔴 emoji 替换为红色 `LIVE` 小字标记，Live 筛选按钮去掉 🔴

- **V6.12.1** — UI 美化 + 文本英文化
  - **表格斑马纹 + hover**：所有表格偶数行加深色背景，hover 橙色高亮
  - **按钮统一 transition**：全部按钮 `transition: all 0.15s ease`，点击反馈更顺滑
  - **输入框 focus 光晕**：`box-shadow: 0 0 0 3px rgba(249,115,22,.15)` 橙色光晕
  - **折叠箭头动画**：`<details>` 自定义 `▶` 箭头，展开旋转 90° 过渡动画
  - **手局批量栏重构**：全选 checkbox 从表头移至批量操作栏，新增"已选 N 项"计数
  - **表头列名更新**：`日期`→`time`，`盈亏(BB)`→`profit`
  - **checkbox 主题色**：`accent-color: #f97316`，16px 统一大小
  - **界面文本英文化**：主导航 → Timer / Odds / Tournament / Review；复盘子导航 → Hand / Session / Weekly / Villain；手局表头 → Time / Type / Session / Hand / Profit / Errors / Edit；"历史手局"→ Hand History；"Hands Note"→ Hand Notes

- **V6.12.0** — GG 导入优化 + Run-it-twice 修复
  - **GG 导入列表重构**：3 处 `innerHTML` 改为 `createContextualFragment` + `replaceChildren`
  - **Run-it-twice 板面解析**：parser 支持 `*** FIRST FLOP ***` / `*** SECOND TURN ***` 格式，Test7 恢复
  - **Bug 修复**：`showIdx`/`sumIdx` 声明修复（var 逗号链拆分陷阱）

## V6.11

- **V6.11.1** — Console 报错修复
  - **CSP 仅生产构建生效**：CSP 从静态 HTML 移入 Vite 插件（`apply: 'build'`），dev 模式不加
  - **CSP 策略补充**：加 `manifest-src 'self' data:`
  - **selfTests 改用 CONSTANTS.DEV**：替代 `import.meta.env.DEV`（Vite 注入异常）
  - **Playwright e2e 增强**：修复监听绑定顺序，CSP 违规在加载阶段触发的问题

- **V6.11.0** — 对手画像增强 + 时间精度
  - **对手别名**：新增 `Store.opponentAliases`，点击 ✎ 编辑昵称，全显示位置统一调用 `Utils.getOpponentDisplayName()`，导出/导入保留
  - **时间精确到分钟**：GG 解析器输出 `YYYY-MM-DD HH:MM`，手动录入用 `getLocalDatetime()`，表格行仅显示日期，展开显示完整时间，向后兼容旧格式
  - **对手详情手牌列表**：展开对手后显示手牌列表（日期/底池类型/盈亏/错误），点击行展开详情，✎ 按钮切到编辑表单
  - **Bug 修复**：`renderOpponentProfiles` 缺 `var self = this`；bindNav 中 `this.Odds.calc()` 改为 `Odds.calc()`

## V6.10

- **V6.10.2** — 文档更新 + 生产优化
  - **ARCHITECTURE.md 重写**：IIFE/单文件 → Vite + 17 ES 模块 + IndexedDB 双后端
  - **selfTests 生产剔除**：`if (CONSTANTS.DEV)` 包裹，生产构建 -10KB

- **V6.10.1** — 批量删除完善
  - **手局批量删除**：勾选后确认删除，含提示，删完重置到第 1 页
  - **GG 导入失败提示**：解析汇总显示成功/失败数

- **V6.10.0** — 对手画像库 V1
  - **对手画像面板**：复盘子 Tab 新增"对手"，按对手名聚合所有 GG 导入手牌。
  - **统计指标**：交手次数、摊牌次数、总盈亏（BB）、胜率、最近交手日期、对手底牌列表。
  - **交互**：点击对手行展开/收起详情。Session 面板的"玩家数据分析"占位已链接到对手面板。
  - **数据来源**：已有 `HandRepo` 中的 `oId`/`oCards`/`pBB` 字段（此前存储但未使用）。

## V6.9

- **V6.9.4** — CSP 内容安全策略（M5 第二弹）
  - **CSP meta 标签**：`default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; worker-src 'self' blob:`。禁止内联脚本/eval/外部资源，Style 暂保留 unsafe-inline（全项目 80+ 处 style 属性迁移工程量大）。
  - **CSP 兼容修复**：`main.js` 错误页 `onclick="location.reload()"` 改为 `addEventListener`。

- **V6.9.3** — 架构收尾：GG 导入提取 + Template 扩大（M5 第一弹）
  - **GG 导入独立模块**：`src/modules/ggImport.js`（~220 行），从 `app.js` init() 中提取 312 行 GG 导入逻辑。
  - **app.js 瘦身**：623 → 314 行。移除未使用的 `GGParser`/`HandRepo` import。
  - **手牌行 Template 渲染**：`renderHandReviews()` 改用 `<template>` 克隆 + `replaceChildren`，与 Session 行一致。

- **V6.9.2** — 架构整理：App 子模块拆分（M4 第二弹）
  - **App 子模块拆分**：`src/modules/app.js`（~2500 行）拆分为 7 个独立文件：
    - `tournament.js` ~5 行 — 锦标赛占位桩
    - `dataSync.js` ~80 行 — 剪切板导入/导出 + CSV
    - `tiltRescue.js` ~90 行 — 情绪急救（用 PubSub 解耦 Review 依赖）
    - `timer.js` ~380 行 — 站立提醒定时器（beep/vibrate/notify 通过依赖注入）
    - `odds.js` ~300 行 — 底池赔率计算器
    - `review.js` ~1050 行 — 复盘系统（Session/手局/周级 + 统计 + 图表）
    - `app.js` ~620 行 — 剩余骨架（init/导航/GG导入/存储健康指示器/SRP/行动线）
  - **PubSub 首次使用**：tiltRescue → Review 通过 `PubSub.emit('tiltLogSaved')` 解耦
  - **Template 渲染试点**：Session 表格行改用 `<template>` 克隆 + `replaceChildren`

- **V6.9.1** — Template 渲染试点 + Bug 修复
  - **Bug 修复**：补齐 M2 拆分时遗漏的 `renderSRPTable()` 和 `renderActionLines()` 函数
  - **Template 渲染**：Session 列表从 innerHTML 改为 `<template>` + `cloneNode` + `DocumentFragment`
  - **新增**：`Utils.fillTemplate()` 模板填充工具，`index.html` 中 3 个 `<template>` 元素

- **V6.9.0** — 性能与健壮性升级（M3 里程碑）
  - **手牌复盘分页**：手牌列表改为分页显示（每页 50 条），上下翻页 + 首末页跳转。解决数千条手牌全量渲染时页面卡顿的问题。
  - **存储健康指示器**：页面标题右侧增加存储状态圆点（🟢 IndexedDB 正常 / 🟡 localStorage 降级 / 🔴 数据异常）。点击可查看存储模式和数据量。
  - **安全模式**：URL 添加 `#safemode` 可跳过 IndexedDB 直接使用 localStorage 启动，用于存储损坏时的应急恢复。
  - **Canvas 响应式**：盈亏图表使用 ResizeObserver 监听容器大小变化，手机旋转或窗口调整时自动重绘。
  - **Service Worker 独立文件**：`public/sw.js` 替代 Blob URL 内联注册，可在 DevTools Application 面板中调试。Blob URL 保留为降级方案。
  - **BaseRepo 增强**：新增 `count()` 和 `getTotalPages()` 方法。

## V6.8

- **V6.8.0** — 工程化升级：Vite + 多文件模块化拆分（M2 里程碑）
  - **Vite 构建系统**：引入 Vite 7 + `vite-plugin-singlefile`，开发时 `npm run dev` 启动热更新服务器，生产构建 `npm run build` 输出单 HTML 文件（`dist/index.html`，140KB / gzip 39KB）。
  - **多文件模块拆分**：从单文件 5898 行 IIFE 拆分为 12 个 ES 模块文件：
    - `src/constants.js` — CONSTANTS + 类型定义 + EQUITY 查找表
    - `src/utils.js` — Utils 工具函数 + PubSub 事件总线
    - `src/parsers/ggParser.js` — GG 手牌历史解析器（独立模块）
    - `src/store/store.js` — Store + IndexedDB + BaseRepo + repos + 迁移逻辑
    - `src/modules/app.js` — App 主控制器（含所有子模块 Timer/Odds/TiltRescue/DataSync/Review）
    - `src/data/srpData.js` / `actionLines.js` — 静态策略数据
    - `src/selfTests.js` — GG 解析器自测
    - `src/main.js` — 入口：import 所有模块 + 启动应用
  - **CSS 独立**：`src/styles.css` 从 HTML `<style>` 标签完全剥离。
  - **零功能变更**：所有模块内容逐行原样提取，无任何逻辑修改。`file://` 兼容性由 Vite 构建产物保证（`base: './'` + singlefile 全内联）。
  - **开发方式变更**：开发阶段使用 `npm run dev`（Vite dev server），不再直接双击 `index.html`。生产部署仍用 `dist/index.html`（单文件）。

## V6.7

- **V6.7.1** — 存储底座升级：localStorage → IndexedDB（M1 里程碑）
  - **IndexedDB 主存储**：新增 `DB` 对象封装 IndexedDB（pa_store 数据库，4 个 Object Store + handReviews 含 3 个索引）。handReviews/sessions/weeklyReviews/tiltLogs 四张大表从 localStorage 迁移到 IndexedDB，突破 5MB 上限。
  - **内存缓存层**：`BaseRepo` 增加内存缓存（`_cache`），`getAll()`/`saveAll()` 操作缓存（同步），后台 300ms 防抖异步写入 IndexedDB。对外 API 签名完全不变，35 个调用点零改动。
  - **自动迁移**：首次启动检测 IndexedDB 为空 → 从 localStorage 逐表迁移 → 校验记录数一致 → 标记 `pa_migrated_v1` → 删除 localStorage 大表。迁移失败保留 localStorage 数据，下次启动重试。
  - **隐私模式兼容**：IndexedDB 不可用时（隐私模式/旧浏览器），所有 Repo 自动退化到 localStorage，功能完全不受影响。
  - **数据完整性**：启动时检测各 Repo 缓存是否为数组，损坏时自动从 localStorage 恢复。导入备份时 Schema 校验（sessions/handReviews 等字段类型必须为数组）。
  - **安全网**：`beforeunload` 事件紧急 flush 缓存到 localStorage，防止页面关闭时 IndexedDB 写入未完成导致数据丢失。

## V6.6

- **V6.6.2** — 架构重构启动版本（为 IndexedDB / TypeScript 做准备）：
  - **仓储模式**：新增 `BaseRepo` 仓储基类（getAll/saveAll/getById/add/update/delete），创建 SessionRepo/HandRepo/WeeklyRepo/TiltLogRepo 四个实例。全局替换 36 处 `Store.xxx.get()/save()` 调用点，统一收口到 Repository。Store 的 settings/timer/standup/logs 配置项保留不动。
  - **JSDoc 类型定义**：为 8 个核心实体（Session/HandReview/WeeklyReview/TiltLog/Settings/LongBreakConfig/TimerState/Standup）添加 `@typedef` 类型注解，启用 VSCode 类型提示。
  - **PubSub 事件总线**：引入 `PubSub.on/off/emit` 轻量发布订阅工具，供后续版本模块解耦使用。
  - **GG 导入去重增强**：重复 handId 不再静默过滤，改为显示黄色 `⚠️ 已存在` 标签 + [覆盖]/[对比] 按钮。覆盖仅更新牌面/盈亏/对手字段，保留用户手动填写的决策与错误类型。对比弹窗并排显示新旧数据差异，差异行黄色高亮。

- **V6.6.1** — UI 优化与修复综合版本：
  - **赔率面板**：几何路线与 MDF 间距缩至 1.5px。标题「底池赔率 & MDF」改为「Pot Odds」。删除结果区「🧾 计算」冗余标题。听牌快捷按钮重排为 OESD→FD→GS+2high→P+FD→Combo Draw，单挑改 HU，对齐修正。快捷下注删除 25%/150%，100%→120%（比例 1→1.2）。
  - **计时器**：删除快捷预设按钮行。默认时长改 20+2。
  - **复盘**：子面板顺序改为手局（默认）→Session→周级。
  - **设置按钮**：右上角小尺寸（24px）半透明，悬停恢复不透明。菜单横向从右至左弹出。
  - **Live 模块**：改名为「玩家数据分析」，预留对手画像子区域。
  - **修复**：计时器卡片 div 未闭合问题；恢复 prettier-ignore 标记。

## V6.5

- **V6.5.5** — 数据瘦身 + 日期列 + 时区修正：手局表格新增「日期」列。GG 手牌时间从 UTC 修正为 UTC+8（北京时间），解决凌晨手牌日期差一天问题。手牌记录字段名压缩（`importedFromGG`→`gg`、`ggHandId`→`ggId`、`opponentId`→`oId`、`opponentCards`→`oCards`、`profitBB`→`pBB`），启动时自动迁移旧数据。
- **V6.5.4** — 手局批量操作 + GG 导入全选：手局表格新增复选框列 + 全选框，支持批量删除和批量关联 Session（选中多手牌 → 选择目标 Session → 一键关联）。GG 导入弹窗新增「全选」「取消全选」按钮方便批量勾选。
- **V6.5.3** — Bug 修复：修复手牌卡片渲染 `Xundefined` 问题（模板占位符 `Xx Xx` 校验失败 → 回退文本显示）。修复对手手牌丢失（摊牌在翻牌圈且不在 SHOWDOWN 区时搜不到 → 扩大 showBlock 搜索范围 + SUMMARY 回退）。修复翻前 fold 丢失（翻前行动收集正则漏 `folds` + `formatOppAct` 缺 fold 分支）。
- **V6.5.2** — 手局历史增强：历史手局表格新增「盈亏(BB)」列（绿色盈/红色亏），旧手牌无数据显示 `--`。「手牌」列改为从 desc 解析 Hero 手牌渲染花色卡片（♠♡♣♢红黑双色），解析失败回退文本。「操作」列改名为「编辑」。GG 导入自动在 reflection 预填盈亏信息（盈利：+XX BB / 亏损：-XX BB）。手动录入默认模板统一为 `preflop 行动：Hero /[Xx Xx]` 格式。handReview 数据结构新增可选字段 `profitBB`（GG 导入时自动存入，手动录入为 null，旧数据兼容）。
- **V6.5.1** — Session-手牌双向穿透 + GG 去重导入：Session 表格操作列新增 📋 展开按钮，点击显示关联手牌迷你列表（类型/描述/错误/编辑）。`addSession()` 保存后自动检测同日期未关联手牌并提示归入。GG 导入解析后过滤已存在的 `ggHandId`，跳过重复牌局并显示提示；全部重复时隐藏导入按钮。

## V6.4

- **V6.4.3** — JSDoc 注解 + 不变量检查 + 级别自动补全：8 个核心函数添加 `/** ... */` 类型注解（GGParser.parse/_calcProfit、getEquity/getRequiredOuts、calcImplied、switchPhase、Store._getRaw/_setRaw）。CONSTANTS 新增 `DEV: true` 开关，Timer 阶段切换和 Store 写操作加入 `console.assert` 不变量检查。Session 级别输入框 `<datalist>` 改为从已保存数据动态提取不重复级别名，新增级别自动出现在补全列表中。
- **V6.4.2** — GG 解析器中等拆分：新建 `GGParser` 独立命名空间，提取 `_splitBlocks`/`_calcProfit`/`_posMap`/`_classifyBoard`/`_sortCardsDesc`/`_getNewCards`/`_formatAction` 7 个函数 + `parse()` 主入口。`Utils.parseGGHandHistory` 保留为薄包装。新增 `#region SelfTests` 包含 4 项自测用例（HU 盈亏、多人 uncalled 扣除、牌面分类、牌面排序）。
- **V6.4.1** — #region 折叠标记：给所有 JS/CSS/HTML 模块添加 VS Code 折叠标记（14 对）。CSS 用 `/* #region */`，HTML 用 `<!-- #region -->`，JS 用 `// #region`。GG 解析器作为 Utils 子区域嵌套，App.Timer/Odds/TiltRescue/DataSync 作为 App 主控制器子区域嵌套。纯工程整理，不影响任何功能逻辑。

## V6.3

- **V6.3.1** — 隐含赔率输入框自动填充：calc() 末尾自动计算默认值填入 `impliedInput`。一张牌（转牌）= min(有效筹码, 河牌底池/2)；两张牌且已填转牌下注金额 = min(有效筹码, turnBet)；其余情况 = 0。联动有效筹码 SPR 上限。不修改 calcImplied()/UI/存储。
- **V6.3.0** — 赔率模块胜率计算升级为精确查表法：新增 EQUITY_FLOP/EQUITY_TURN 两张预计算表（0-20 outs，翻牌用组合概率公式、转牌用 outs/46），淘汰 4/2 法则（大 outs 误差可达 6%）。Utils 新增 getEquity/getRequiredOuts 方法，平衡所需 Outs 反向查表。新增多人底池修正：outs 同行增加 non-HU 选择器（单挑/2人/多人），2 人修正系数 0.96、多人 0.91。结果显示区改为「胜率 XX% (精确) → 多人修正 ≈ YY%」。outs 快捷按钮英文缩写：GS+2high/OESD/FD/P+FD/Combo Draw。

## V6.2

- **V6.2.2** — GG 解析器盈亏计算完全重写：废弃跨局筹码差值路径（source=nextHand 导致翻前弃牌手牌显示 -51.6BB），改为纯投入法。每局独立计算 invested = 强制投入(盲注+ante) + 主动投入(calls/bets/raises) - 未跟注返还。胜负判定统一为三条路径：SHOWDOWN collected / SUMMARY won / 其他(profit=0-invested)。变量 invested 在每局循环体内 var 声明确保隔离。删除所有回退逻辑和诊断日志。
- **V6.2.1** — 手牌实时可视化：在 handDesc 上方新增牌面卡片展示区，按街道分行渲染 + Hero/对手手牌同行。支持 `牌面：` 模板格式和 GG 导入格式（无 `牌面：`）双路径提取。自动推断牌面结构（M/TT/R）。150ms 防抖，编辑回填/保存清空时自动触发刷新。
- **V6.2** — GG 解析器盈亏回退增强 + 行动线格式优化：翻前行按实际顺序排列；转牌/河牌仅显示新增牌；牌面按 rank 降序排列；下注同时显示 bb 值和底池百分比；摊牌结果每个对手独立成行。Hero 牌面卡片显示花色符号颜色。

## V6.1

- **V6.1.1** — GG 解析器修复：块分割改用 `Poker Hand #` 切割（修复街间空行导致翻后丢失）；HU 位置映射（BTN=SB, BB）；多人底池全部对手动作收集；翻前 Call 保持无金额格式
- **V6.1** — GG 解析器行动线重构：动态位置映射（BTN/SB/BB/UTG/MP/HJ/CO，自适应 2-9 人）；下注改为底池百分比优先（B30/B75），无法计算时退化为 bb 值；翻前精简为 `Hero 位置/(手牌) 动作, 对手位置 Call/Raise`；翻后仅显示 Hero 与直接对手交互行；摊牌追加 `shows [手牌] (牌力)`；无关玩家 Fold/盲注行过滤

## V6.0

- **V6.0** — GG 手牌历史解析导入：手局面板新增「📥 从 GG 牌谱导入」按钮。粘贴 GG 手牌历史文本自动解析为 OTF/OTT/OTR 格式，大底池亏损（>40BB）红色高亮默认勾选。导入前校验 localStorage 容量。handReviews 新增 `importedFromGG`/`ggHandId`/`opponentId`/`opponentCards` 可选字段，完全向后兼容

## V5.8

- **V5.8.3** — 赔率面板布局优化：尺度速查表改为折叠面板；后续再下注移到牌数行同排；MDF与连续防守概率合并展示；Bet/Pot与需要胜率同行，EV与+EV/-EV结论同行
- **V5.8.2** — 赔率面板信息合并：Alpha 面板与常见下注尺度速查表合并为常显紧凑表格（尺度|所需胜率|Alpha|MDF），置于下注比例高亮行下方；非标准尺度在两行之间显示黄色分割线
- **V5.8.1** — 手局/周复盘编辑 + 休息跳过 + 增量导入：手局和周复盘新增编辑按钮；休息阶段显示「跳过休息」按钮直接切回工作；导入备份改为 UUID/时间戳合并去重，保留本地单例状态
- **V5.8** — 合并 V5.7.5 优化 + BUG 修复：声音/振动偏好持久化（`Store.settings`）；全局输入框自动全选；修复 `roundRect` 浏览器兼容崩溃；恢复跨周错误对比、长休息金色光晕、版本标签动态化等 V5.7.4 回归；`exportAll/importAll` 补齐 settings

## V5.7 系列

- **V5.7.4** — 版本标签动态化 + 跨周错误对比 + 长休息视觉反馈：HTML 中版本号改为完全由 JS 设置；周统计新增 vs 上周错误趋势对比；长休息时计时器数字闪烁金色光晕
- **V5.7.3** — 数字卡片化 + 响应式表格 + 听牌按钮缩小：统计区盈亏/bb100/时薪加绿红渐变背景微光；窄屏(<400px)自动隐藏 Session 表格次要列；听牌快捷按钮改为单行紧凑布局
- **V5.7.2** — UI 折叠优化：Session 表单的错误类型/情绪/备注收进「更多选项」折叠区；计时器长休息设置默认折叠，摘要行显示当前配置状态
- **V5.7.1** — 修复分享网站按钮：添加简洁弹窗显示网址 + 一键复制，兼容 file:// 和 https://
- **V5.7** — 移除 QR 码分享功能：删除内联 QRCode 库（~15KB）、`Utils.generateQR()`、QR 模态弹窗及相关 CSS

## V5.6 系列

- **V5.6.4** — 盈亏趋势图：统计区新增 Canvas 柱状图（绿盈红亏）+ 3 场滑动平均线
- **V5.6.3** — CSV 导出：迁徙菜单新增「导出CSV」，导出 Session 数据为 CSV 文件（含 BOM 兼容 Excel）
- **V5.6.2** — Session 编辑：支持点击 ✎ 编辑已有 Session 记录，回填表单后更新保存
- **V5.6.1** — 番茄钟长休息：每完成 N 次专注后触发一次长休息（默认 4 次→15 分），新增 15+2 预设

## V5.5 系列

- **V5.5.1** — 基线版本

## V5.4 系列

- **V5.4.3** — 修复 Service Worker 缓存重复、迁徙菜单绑定位置、formatTime 无效日期保护
- **V5.4.2** — 上一稳定版
